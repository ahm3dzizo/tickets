const xlsx = require('xlsx'); const wb = xlsx.readFile('NTF1 Ticket (1).xlsm'); const ws = wb.Sheets[wb.SheetNames[0]]; const data = xlsx.utils.sheet_to_json(ws); let missingId = 0; const ids = new Set(); for (const row of data) { const id = row['??? ???????'] || row['ID'] || row['??? ?????']; if (!id) missingId++; else ids.add(id); } console.log('Total Rows:', data.length, 'Missing ID:', missingId, 'Unique IDs:', ids.size);
