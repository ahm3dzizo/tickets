import { PrismaClient } from '@prisma/client'; const prisma = new PrismaClient(); async function run() { const p = await prisma.project.findFirst({ where: { name: { contains: 'النرجس' } } }); if (!p) return; const sample = await prisma.ticket.findMany({ where: { projectId: p.id, type: 'plumbing' }, take: 30, select: { description: true, type: true }}); sample.forEach((s, i) => console.log((i+1) + '. ' + s.description)); } run().finally(() => prisma.$disconnect());
