// Firebase Auth was planned but the app uses its own JWT auth system instead.
// This file is kept as a stub so future Firebase features can be added here.
// FCM push notifications are handled via Web Push (server/pushService.ts) without Firebase.
export {};
