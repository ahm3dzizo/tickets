const xlsx = require('xlsx'); const wb = xlsx.readFile('NTF1 Ticket (1).xlsm'); const ws = wb.Sheets[wb.SheetNames[0]]; const data = xlsx.utils.sheet_to_json(ws); const ids = new Set(); let empty = 0; let dups = 0; for(let i=1; i<data.length; i++) { const id = data[i]['__EMPTY']; if(!id) empty++; else if(ids.has(id)) dups++; else ids.add(id); } console.log('Rows:', data.length, 'Empty:', empty, 'Dups:', dups, 'Unique:', ids.size);
